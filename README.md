#💡 
脚本说明
一键脚本：



执行过上面的一键命令后，脚本文件会保存在你当前目录下的 mmx_manager.sh。

以后如果需要再次打开管理菜单（如重启或看日志），只需要在终端执行以下命令即可：

Bash
./mmx_manager.sh
🛠️ 常见注意事项：
防火墙放行：安装完成后，如果打不开网页，请前往你的云服务器厂商后台（如腾讯云、阿里云、国外 VPS 厂商等），在安全组/防火墙中入站规则放行你刚刚设置的端口（默认 12889）。

更新：如果以后作者发布了新版本，直接再次运行脚本选择 1（安装 / 更新 妙妙屋X）即可，它会自动拉取最新 Docker 镜像并覆盖升级，不会丢失已有数据。
